
@startuml
|User|
start
:Start Identification;
if (Identification successful?) then (yes)
    |User|
    :Initialize Demo;
    |Indy|
    :Initialize Wallets;
    :Create Credential Offers;
    :Create Proof-Requests;
    |User|
    :Request Identity-Credential;
    |Indy|
    :Identity-Credential;
    |User|
    :Request Degree-Credential;
    |Indy|
    :Create Proof for \n Degree-Credential;
    :Degree-Credential;
    |User|
    :Apply for Job;
    |Indy|
    :Create Proof for \n Job-Application;
    :Job-Credential;
    stop

else (no)
    |User|
    stop
@enduml


